package chatcessibility.chatcessibility;
import org.bukkit.util.io.BukkitObjectInputStream;

import java.io.ObjectInputStream;
import java.util.function.Function;

public class contextEvaluationFunction {
    /*
    This project requires this file as it is needed to process
    any and all server input via command line. When the server
    sends a command over the internet. the plugin will listen to
    chat to make sure that the user is able to essentially
    receive the accessibility service correctly. this file is
    responsible for everything
    */
     
}
